node_modules/
dist/

# secrets — never commit wallet seeds / keys / RPC keys
.env
.env.*
!.env.example
*.seed
*.key

# QVAC model cache (large binaries fetched at runtime)
models/
.qvac/
*.gguf
*.onnx
*.bin

# generated TTS output (regenerated by Spike C; keep the input WAV committed)
spikes/audio/*-out.wav

# logs & scratch
*.log
tmp/

# local agent/editor tooling (not part of the public project)
.claude/

# per-instance wallet seeds (secrets)
.data/
